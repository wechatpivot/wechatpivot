import { mapGetters, mapActions } from 'vuex'
import './{{RouteName}}View/styles.css';


export default {
  name: '{{RouteName}}View',

  computed: mapGetters([
    'counter',
  ]),

  methods: mapActions([
    'increment',
  ]),

  render(h) {
    return (
      <div class="{{RouteName}}">
        {{RouteName}}View
      </div>
    );
  } ,
};
